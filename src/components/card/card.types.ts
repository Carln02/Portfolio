export type PortfolioCardData = {
    images?: string[];

    title?: string;
    location?: string;

    startDate?: Date,
    endDate?: Date,

    link?: string;
    linkText?: string;

    tags?: string[];

    description?: string;
}